"""Application configuration for events application."""

from django.apps import AppConfig


class EventsAppConfig(AppConfig):
    """Application configuration for events application."""

    name = 'events'
    verbose_name = 'Event Hub'
