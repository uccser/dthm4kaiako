"""Application configuration for DTTA application."""

from django.apps import AppConfig


class DttaAppConfig(AppConfig):
    """Application configuration for DTTA application."""

    name = 'dtta'
    verbose_name = 'DTTA'
